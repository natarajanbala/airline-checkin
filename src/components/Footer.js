import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <h1>
            Footer
        </h1>
    );
};

Footer.propTypes = {
    
};

export default Footer;