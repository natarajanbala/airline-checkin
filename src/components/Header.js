import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
    return (
        <h1>Airline Check-in System</h1>
    );
};

Header.propTypes = {
    
};

export default Header;